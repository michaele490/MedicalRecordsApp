<p style="color: green"><%= notice %></p>

<%= render @patient %>

<div>
  <%= link_to "Edit this patient", edit_patient_path(@patient), class:'btn btn-primary me-3' %>
  <%= link_to "Back to patients", patients_path, class:'btn btn-primary' %>

  <%= button_to "Delete patient", @patient, method: :delete, class:'btn btn-danger mt-3' %>
</div>
